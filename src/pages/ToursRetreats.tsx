import { Navigate } from "react-router-dom";

const ToursRetreats = () => {
  // Redirect to the Annual Omni Wellness Retreat detail page
  return <Navigate to="/tour-detail/winter-wine-country-wellness" replace />;
};

export default ToursRetreats;
